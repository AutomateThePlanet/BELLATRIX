Your Tests project need to intall Bellatrix.ImageRecognition.SikuliX nuget package
All test images should be added as embedded resources
In the AssemblyCleanup you should call Screen?.Dispose();

You need to install Java 64 version 8.0 or higher
JAVA_HOME per Machine should point to the java.exe 64 bit version.